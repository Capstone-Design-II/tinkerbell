
export const NAMESPACE = 'user'

const ID = 'id'
const NAME = 'name'

export const state = {
  ID,
  NAME
}

const INIT = 'init'
const SET_ID = 'setId'
const SET_NAME = 'setName'
const SET_ALL = 'setAll'

export const mutations = {
  INIT,
  SET_NAME,
  SET_ID,
  SET_ALL
}

const REGISTER_VOICE = 'register_voice'
const LOGIN = 'login'

export const actions = {
  REGISTER_VOICE,
  LOGIN
}
